{
  "name": "community-navigator-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "build:snapshot": "vite build --mode snapshot && node ../scripts/check-size.mjs dist/index.html"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^7.3.6",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
