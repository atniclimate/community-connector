SYNTHETIC DATA ONLY. All example emails must use the @example.test namespace, and no real person's data may appear here. Two contrasting demo groups land in Phase 1.

G1 vocabulary quarantine (D-044.4): fixtures never contain ATNI-plausible capability terms; synthetic vocabulary stays inside the two existing demo domains (research-network, fisheries-committee).

`groups/atni-convention.ops.jsonl` is generated by `app/scripts/generate-atni-ops.mjs`
(never hand-edited). Since CS-02 (D-103 pick 2), person-to-person `connected_to` edges
are derived deterministically from overlapping `areas_of_interest` tags (2+ shared
tags), not from an arbitrary index formula; `app/scripts/check-atni-edges.mjs` verifies
that invariant, prints edge counts per kind, and reports the designated spotlight
person's neighbor count. Regenerate with `node app/scripts/generate-atni-ops.mjs`, then
check with `node app/scripts/check-atni-edges.mjs`.
